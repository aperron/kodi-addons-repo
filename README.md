python -m pip install pip --upgrade --user
python -m pip install pipenv --user
# add Python Scripts fodler, if doesn't
pipenv install

pipenv run .\addons_xml_generator.py aperron/aperron-kodi-a
ddons-repo